class ApplicationController < ActionController::Base
  # This application only uses the article 'index' endpoint and reaction 'like' endpoint.
  # Other CRUD endpoints included have been left as created for illustration only.
end
